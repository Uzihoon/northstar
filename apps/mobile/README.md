# Northstar Mobile

Mobile app workspace for Northstar.

This app will call the backend API in `../api`, starting with:

- `GET /destinations`
- `POST /itinerary-plan-runs`
- `GET /itinerary-plan-runs/{run_id}`
- `GET /itinerary-plans/{plan_id}/summary`
